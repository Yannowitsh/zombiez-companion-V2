# Gradle / Loom build outputs
.gradle/
build/
out/
!gradle/wrapper/gradle-wrapper.jar

# Loom
.loom-cache/
run/
*.log

# IDE
.idea/
*.iml
*.ipr
*.iws
.vscode/
.settings/
.classpath
.project
bin/
eclipse/

# OS
.DS_Store
Thumbs.db

# Dev-only, never committed (personal planning / captured logs)
IDEAS.md
latest.log

# Reference mods / configs from other authors (inspiration only, never committed)
/*.jar
play.rinaorc.com.json

# Local build archive (kept for personal version history, never committed)
Versions/

# Cloudflare Worker (backend) — local cache / deps / dev secrets
backend/.wrangler/
backend/node_modules/
backend/.dev.vars
